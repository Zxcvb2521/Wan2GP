import React,{useEffect,useMemo,useState}from'react';import{CheckCircle2,Download,HardDrive,LoaderCircle,Search,Trash2,XCircle,AlertTriangle,Info}from'lucide-react';
const API='http://127.0.0.1:18765';type C={compatible:boolean;level:'recommended'|'possible'|'unsupported';reasons:string[]};type F={filename:string;size_bytes:number;sha256:string;url:string};type M={id:string;name:string;kind:string;description:string;sizeGb:number;state:string;compatibility?:C;files?:F[]};type I={model_id:string;path:string;filename:string;sha256:string;verified:boolean};const labels:Record<string,string>={image:'Изображения',video:'Видео',llm:'Текст / LLM',tts:'Голос / TTS',music:'Музыка'};
export default function ModelManager(){const[models,setModels]=useState<M[]>([]),[installed,setInstalled]=useState<I[]>([]),[query,setQuery]=useState(''),[kind,setKind]=useState('all'),[busy,setBusy]=useState<string|null>(null),[error,setError]=useState(''),[notice,setNotice]=useState('');async function load(){try{const[m,i]=await Promise.all([fetch(`${API}/models`).then(r=>r.json()),fetch(`${API}/models/installed`).then(r=>r.json())]);if(m.ok)setModels(m.models||[]);if(i.ok)setInstalled(i.models||[]);setError('')}catch(e){setError(e instanceof Error?e.message:'Не удалось получить список моделей')}}useEffect(()=>{load()},[]);const visible=useMemo(()=>models.filter(m=>(kind==='all'||m.kind===kind)&&`${m.name} ${m.description}`.toLowerCase().includes(query.toLowerCase())),[models,kind,query]);const ready=(id:string)=>installed.some(x=>x.model_id===id);async function remove(id:string){setBusy(id);setNotice('');try{const r=await fetch(`${API}/models/${id}`,{method:'DELETE'}),d=await r.json();if(!r.ok)throw new Error(d.error||'Не удалось удалить модель');setNotice('Модель удалена.');await load()}catch(e){setError(e instanceof Error?e.message:'Ошибка удаления')}finally{setBusy(null)}}async function install(m:M){const file=m.files?.[0];if(!file){setNotice('Для этой модели пока не указан проверенный источник загрузки.');return}setBusy(m.id);setError('');setNotice('Подготовка загрузки…');try{const r=await fetch(`${API}/models/install`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model_id:m.id,url:file.url,filename:file.filename,sha256:file.sha256||undefined})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Не удалось установить модель');setNotice(`Модель «${m.name}» установлена и проверена.`);await load()}catch(e){setError(e instanceof Error?e.message:'Ошибка установки')}finally{setBusy(null)}}return <section className="model-manager"><div className="creator-card"><div className="creator-head"><div><span className="eyebrow">БИБЛИОТЕКА AI</span><h2>Модели</h2><p className="muted">Локальные модели и их совместимость с этим компьютером.</p></div><button className="ghost"onClick={load}>Обновить</button></div><div className="model-toolbar"><div className="search-box"><Search size={17}/><input value={query}onChange={e=>setQuery(e.target.value)}placeholder="Найти модель…"/></div><div className="model-filters"><button className={kind==='all'?'active':''}onClick={()=>setKind('all')}>Все</button>{Object.entries(labels).map(([id,label])=><button key={id}className={kind===id?'active':''}onClick={()=>setKind(id)}>{label}</button>)}</div></div>{notice&&<div className="notice-box"><Info size={16}/>{notice}</div>}{error&&<div className="error-box">{error}</div>}<div className="model-grid">{visible.map(m=>{const c=m.compatibility,s= c?.level||'possible',ok=ready(m.id),hasSource=Boolean(m.files?.length&&m.files[0].url);return <article className="model-card"key={m.id}><div className="model-card-head"><div><span className="model-kind">{labels[m.kind]||m.kind}</span><h3>{m.name}</h3></div>{ok?<span className="model-badge good"><CheckCircle2 size={14}/>Установлена</span>:s==='recommended'?<span className="model-badge good"><CheckCircle2 size={14}/>Подходит</span>:s==='possible'?<span className="model-badge warn"><AlertTriangle size={14}/>С ограничениями</span>:<span className="model-badge bad"><XCircle size={14}/>Не подходит</span>}</div><p>{m.description}</p><div className="model-meta"><span><HardDrive size={14}/>{m.sizeGb?`${m.sizeGb.toFixed(1)} ГБ`:'Размер уточняется'}</span>{c?.reasons?.[0]&&<span title={c.reasons.join(' ')}>{c.reasons[0]}</span>}</div><div className="model-card-actions">{ok?<button className="ghost danger"disabled={busy===m.id}onClick={()=>remove(m.id)}>{busy===m.id?<LoaderCircle size={15}className="spin"/>:<Trash2 size={15}/>}Удалить</button>:<button className="generate"disabled={s==='unsupported'||busy===m.id||!hasSource}title={!hasSource?'Источник загрузки ещё не подключён':''}onClick={()=>install(m)}>{busy===m.id?<><LoaderCircle size={15}className="spin"/>Установка…</>:<><Download size={15}/>{hasSource?'Установить':'Источник не подключён'}</>}</button>}</div></article>})}</div>{!visible.length&&<div className="timeline-empty">Модели не найдены.</div>}</div></section>}
